Tags: #Backpropagation #IntelligentSystems 

## The Backpropagation Algorithm

1. Weight Initialization: Set all weights and node threshold to small random numbers. *Note that the weight from the bias unit (whose activation level is fixed at 1)*.